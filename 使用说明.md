# YouTube 频道邮箱采集工具使用说明

## 1. 软件用途

本工具按关键词搜索 YouTube 公开视频及其频道，整理频道的公开资料，并重点采集频道主动公开的联系邮箱。

工具提供两种采集方式：

- **官方 API**：速度快、稳定，需要 YouTube Data API v3 Key。适合批量获取频道、视频、订阅数等结构化数据。
- **浏览器爬虫**：无需 API Key，使用电脑上已安装的 Chrome 或 Edge 读取公开页面。适合查找频道简介、公开外链、官网邮箱，以及识别需要登录或 reCAPTCHA 才能查看邮箱的频道。

工具可以复用你首次手动建立的 Google/YouTube 登录会话，但不会保存账号密码、自动填写登录信息或绕过验证码，也不尝试获取未公开邮箱。

如需登录，点击界面中的“首次登录 / 更新登录状态”，在打开的独立浏览器中手动完成 Google/YouTube 登录，然后关闭整个浏览器窗口。登录 Cookie 保存在程序目录的 `browser_profile` 文件夹；登录失效时重复此操作即可。登录窗口与采集任务不能同时运行。

程序会检查公开主页简介、公开链接和公开官网。检测到“查看电子邮件地址”时只记录“需人工验证”状态，不会点击按钮或触发 reCAPTCHA，以免消耗 YouTube 的邮箱查看次数。

## 2. 启动软件

1. 解压整个发布包，不要只把 EXE 单独拖出来。
2. 双击 `YouTubeCollector.exe`。
3. Windows 首次运行若显示安全提示，可选择“更多信息”后确认运行。
4. 软件会在当前目录自动使用或创建：
   - `config.json`：本机配置和 API Key。
   - `output`：默认 CSV 输出目录。
   - `logs`：程序运行和错误日志。

免安装版要求电脑已安装 Chrome 或 Edge，只有使用“浏览器爬虫”时才会调用浏览器。

## 3. 界面参数

### 采集方式

- **官方 API（速度快、需 Key）**：调用 YouTube Data API v3。
- **浏览器爬虫（速度慢、无需 Key）**：访问 YouTube 公开网页和频道公开官网。

### YouTube API Key

只有官方 API 模式需要。Key 保存在同目录的 `config.json` 中，不写入 CSV 和日志。发布包内已配置时可直接使用；如 Key 失效，可在界面中替换并点击“保存配置”。

建议在 Google Cloud 中把 Key 限制为仅可访问 YouTube Data API v3。发布包及 `config.json` 不要转发给无关人员；如曾泄露，应立即更换 Key。

### 搜索关键词

多个关键词用半角竖线 `|` 分隔，例如：

```text
比特币|区块链|加密货币
```

关键词越多，请求量、采集时间和 API 配额消耗越大。首次测试建议只填一个关键词。

### 国家/地区

多个国家或地区用 `|` 分隔。可填写两位代码或程序能识别的中英文名称，例如：

```text
US|GB
```

不需要地区限制时留空。频道没有公开地区时，地区筛选可能把该频道排除，因此邮箱采集通常建议留空。

### 粉丝数范围

- 左侧：最低订阅数。
- 右侧：最高订阅数，填 `0` 表示不限制上限。

部分频道会隐藏订阅数，此时程序可能记录为 `0`。

### 每个关键词采集页数

- 填 `1`：适合首次测试。
- 填正整数：限制每个关键词的页数。
- 填 `-1`：持续请求到没有下一页。API 模式会大量消耗配额，爬虫模式也会明显延长运行时间，请谨慎使用。

浏览器爬虫模式中，一页最多按约 50 条搜索结果处理。

### 显示浏览器窗口

仅对浏览器爬虫有效。勾选后可观察网页采集过程，适合排查页面加载、地区限制或验证页面；正常批量运行可不勾选。

### 只保存有邮箱或需人工验证的频道

勾选后，CSV 只保留以下频道：

- 已找到公开邮箱；或
- 页面显示存在商务邮箱，但需要登录、点击验证或完成 reCAPTCHA 后才能查看。

注意：官方 API 不会返回 YouTube 的人工验证入口，因此重点收集邮箱并标注人工验证时，推荐使用浏览器爬虫。

### 简介无邮箱时检查频道公开官网

勾选后，程序会在频道简介没有邮箱时检查频道主动公开的外链，并限量访问同站官网首页、Contact、About 等页面。程序会跳过社交平台、内网地址、非 HTML 页面和过大文件。

## 4. 推荐操作流程

### 快速获取频道数据

1. 选择“官方 API”。
2. 确认 API Key 已填写。
3. 输入关键词。
4. 国家/地区按需填写；只关注邮箱时建议留空。
5. 首次把页数设为 `1`。
6. 选择 CSV 输出位置。
7. 点击“开始采集”。

### 重点获取频道邮箱

1. 选择“浏览器爬虫”。
2. 输入一个关键词，页数先设为 `1`。
3. 国家/地区建议留空，避免漏掉未公开地区的频道。
4. 勾选“只保存有邮箱或需人工验证的频道”。
5. 勾选“简介无邮箱时检查频道公开官网”。
6. 首次可勾选“显示浏览器窗口”观察流程。
7. 点击“开始采集”。
8. 根据 CSV 的“邮箱状态”处理结果。

运行中可点击“停止”。程序采用逐条写入方式，停止前已经写入的结果会保留在 CSV 中。

## 5. 邮箱状态说明

| 邮箱状态 | 含义 | 建议处理 |
| --- | --- | --- |
| 已获取 | 已从频道简介、公开链接或官网找到邮箱 | 可在“联系详情”中查看 |
| 需人工验证（登录/reCAPTCHA） | 页面显示存在邮箱，但必须人工登录或验证 | 打开该行“频道链接”手动处理 |
| 已获取；另有需人工验证的商务邮箱 | 已找到公开邮箱，同时还有一个受验证保护的邮箱入口 | 优先使用已获取邮箱，必要时再人工验证 |
| 未发现 | 已检查的公开信息中没有找到邮箱 | 可后续人工复核 |
| 未发现（API 模式无法判断人工验证入口） | API 数据没有邮箱，且 API 无法识别页面验证入口 | 改用浏览器爬虫复查 |

程序不会自动解决 reCAPTCHA，也不会绕过登录限制。需要验证的频道只做标记。

## 6. CSV 结果

CSV 使用 UTF-8 BOM 编码，可直接用 Excel 打开，中文不会乱码。主要字段包括：

- 搜索关键词、频道名称、频道 ID、频道链接。
- 频道简介、频道国家/地区、订阅数、视频数、总观看数。
- 匹配视频标题、视频链接、发布时间和观看数据。
- 联系类型、联系详情、邮箱状态。

同一关键词下，同一频道会去重；不同关键词命中的同一频道可能分别记录，以便保留关键词来源。

## 7. 常见问题

### 点击开始后日志长时间没有变化

先等待数十秒，爬虫首次启动浏览器或访问官网可能较慢。如果仍无变化：

1. 查看软件目录下 `logs` 中当天的日志。
2. 将每个关键词页数改为 `1`。
3. 国家/地区留空后重试。
4. API 模式检查 Key、YouTube Data API v3 是否启用以及当日配额。
5. 爬虫模式勾选“显示浏览器窗口”，确认页面能否正常打开。

### API 报国家/地区无法识别

改用两位地区代码，例如美国使用 `US`、英国使用 `GB`；或者留空取消地区限制。邮箱采集建议留空，因为很多频道没有公开地区。

### API 模式没有发现邮箱

YouTube API 只提供频道公开简介，不提供“查看电子邮件地址”的人工验证状态。请切换到浏览器爬虫，并勾选公开官网检查。

### 爬虫速度慢

这是正常情况。它需要逐个打开频道资料和公开官网，还会遵守访问间隔。可减少关键词或页数；只测试流程时使用一个关键词、一页。

### 没有生成 CSV

确认已选择可写入的输出路径。若程序在写入第一条记录前发生参数、网络或 API 错误，CSV 可能尚未创建，请查看运行日志和 `logs` 目录。

### CSV 被 Excel 占用

采集时不要同时用 Excel 打开同一个输出文件，否则 Windows 可能阻止程序继续写入。关闭 Excel 后重新采集，或选择新的文件名。

## 8. 备份与迁移

- 迁移软件时复制整个文件夹，才能保留配置和输出。
- 只备份数据时复制 `output` 目录即可。
- 分享软件给他人前，应删除 `config.json` 或清空其中的 API Key，改为提供 `config.example.json`。
