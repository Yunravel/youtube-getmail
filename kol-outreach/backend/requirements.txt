fastapi>=0.110.0
uvicorn[standard]>=0.27.0
sqlalchemy>=2.0.0
pydantic>=2.6.0
python-dotenv>=1.0.0
python-multipart>=0.0.9

# OpenAI
openai>=1.12.0

# HTTP client for external APIs
httpx>=0.27.0

# Excel 读写（xlsx 导入/导出，HotLead 报价单生成）
openpyxl>=3.1.0

# DNS 解析（采集器 MX 校验：services/crawler/）
dnspython>=2.6.0

# 浏览器渲染（采集器官网深度邮箱：services/crawler/fetcher.py PlaywrightFetcher）
# 可选——只有启用 CRAWLER_ENABLE_DEEP_EMAIL=true 时才需要。
# 安装后还要跑 `playwright install chromium` 下载浏览器。
playwright>=1.40.0

# PostgreSQL driver for production
psycopg2-binary>=2.9.0

# Database migrations (生产 schema 变更走 alembic，不用 create_all/启动时 ALTER)
alembic>=1.13.0

# Scheduled and recovery jobs
apscheduler>=3.10.0

# IMAP 附件同步（直连 Gmail 抓真附件，绕开 Snov）
# PySocks 让 imaplib 走 SOCKS5 代理（Clash），cryptography 加密邮箱应用专用密码
PySocks>=1.7.1
cryptography>=41.0

# Quote cards: selectable PDF text + OCR fallback for scanned PDFs/images.
pypdf>=5.0.0
PyMuPDF>=1.24.0
Pillow>=10.0.0
pytesseract>=0.3.13
