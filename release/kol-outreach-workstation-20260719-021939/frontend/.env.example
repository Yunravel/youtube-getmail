# 邮箱页“新邮件”按钮跳转地址；留空时按钮保持禁用。
VITE_SNOV_INBOX_URL=https://app.snov.io/

# 采集页鉴权 token：必须与后端 CRAWLER_TOKEN 完全一致。
# 构建期注入前端 bundle，访问 X-Crawler-Token 头带上。
# 注意：token 会出现在前端代码里，所以它只挡公网随机扫描/跨域调用，
# 真正的隔离靠部署层（私有域名 + 内网）。前后端必须同步设置。
VITE_CRAWLER_TOKEN=replace-with-a-strong-random-token
